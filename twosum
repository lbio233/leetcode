# twosum

class Solution:
    def twoSum(self,nums,target):
        d = {}
        for i,num in enumerate(nums):
            if (target-sum) in d:
                return (d[target-sum],i)
            else:
                d[target-sum] = i
